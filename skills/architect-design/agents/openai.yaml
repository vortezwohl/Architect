interface:
  display_name: "Architect Design"
  short_description: "Manual-only: approve one design bundle"
  default_prompt: "Run only when the user explicitly invokes $architect-design. First build the minimum required understanding of the current codebase, then study source-article.md and supporting external evidence, ask the user to define the compatibility boundary, and recommend the globally best supported design as one approved design bundle containing one or more D-xxx subdesigns. Do not auto-invoke $architect-propose or $architect-build."
