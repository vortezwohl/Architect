interface:
  display_name: "Architect Propose"
  short_description: "Create a sealed Markdown-first implementation contract"
  default_prompt: "Use $architect-propose to turn approved D-xxx design units into a sealed Markdown-first plan package with atomic T-xxx tasks and centralized execution state."
