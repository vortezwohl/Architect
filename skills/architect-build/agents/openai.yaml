interface:
  display_name: "Architect Build"
  short_description: "Build sealed tasks with checkpoint rollback"
  default_prompt: "Use $architect-build to preflight a sealed plan, execute one T-xxx task inside its exact boundary, verify scope after each atomic edit, and rollback every breach."
